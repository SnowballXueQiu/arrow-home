# MySQL
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=arrow_home
MYSQL_USER=arrow
MYSQL_PASSWORD=

# Backend
DATABASE_URL=mysql+pymysql://arrow:${MYSQL_PASSWORD}@db:3306/arrow_home
ADMIN_USERNAME=admin
# Generate hash: python3 -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
# IMPORTANT: replace every $ in the hash with $$ (e.g. $2b$12$... → $$2b$$12$$...)
ADMIN_PASSWORD_HASH=

# Backstage
BACKEND_URL=http://backend:8000
